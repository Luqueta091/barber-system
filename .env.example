# Runtime (Prisma Accelerate / Data Proxy)
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_ACCELERATE_API_KEY"

# Migrations (direct Postgres connection, NOT Data Proxy)
DIRECT_DATABASE_URL="postgres://user:password@host:port/dbname?sslmode=require"

# API
PORT=3334
NODE_ENV=production
